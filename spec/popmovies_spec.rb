require 'spec_helper'

describe Popmovies do
  it 'has a version number' do
    expect(Popmovies::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
